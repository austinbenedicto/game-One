package Cards;

public class CardFactory
{
    /*
    public static Card createCard(CardData data) {
        switch (data.getType()) {
            case "Monster":
                return new Monster(data);
            case "Spell":
                return new Spell(data);
            default:
                throw new IllegalArgumentException("Unknown card type: " + data.getType());
        }
    }
        */
}
